[build]
  command = "cd frontend && npm ci && npm run build && npm run export"
  publish = "frontend/out"

[build.environment]
  NODE_ENV = "production"
  NEXT_PUBLIC_BACKEND_URL = "http://127.0.0.1:8005"
